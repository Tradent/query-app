"use client"

interface SearchFiltersProps {
  query: string
  inChatInterface?: boolean
}

export function SearchFilters({ query, inChatInterface = false }: SearchFiltersProps) {
  // This component has been disabled as requested
  return null
}
